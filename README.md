# Relace Public Documentation

## Development

Install the mintlify CLI tool:

```sh
npm install -g mint
```

Build and deploy the page to localhost:

```sh
mintlify dev
```

## Deployment

Automatically publishes to https://docs.relace.ai/ when pushing to the main branch.
